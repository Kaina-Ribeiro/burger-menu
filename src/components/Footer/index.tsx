export default function Footer() {
  return (
    <footer className="absolute bottom-0 w-full max-h-[147px] min-h-[67px] backdrop-blur-md bg-white/30 z-10 px-6">
      <div className="flex flex-col gap-[10px] items-center justify-center">
        <button className="bg-primary max-w-[432px] w-full h-12 rounded-[40px] text-white font-medium text-lg">
          Add to Order • CONTEXT
        </button>
      </div>
    </footer>
  );
}
